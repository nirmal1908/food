function orderNow(itemName) {
    // Add logic here for ordering the selected item
    alert('Order placed for ' + itemName);
  }
  function AddCart(itemName) {
    // Add logic here for ordering the selected item
    alert('Add Food In Bag ' + itemName);
  }
  